// Nothing to see here today :/

const readFile = require("../support/utils");

const main = () => {
  const lines = readFile("day03/input.txt").split("\n");
  return "Not Implemented";
};

console.info(main());
